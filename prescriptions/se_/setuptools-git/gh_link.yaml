units:
  wraps:
    - name: SetuptoolsGitGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: setuptools-git
      run:
        justification:
          - type: INFO
            message: "Package 'setuptools-git' is hosted on GitHub"
            link: "https://github.com/msabramo/setuptools-git"
            package_name: setuptools-git
