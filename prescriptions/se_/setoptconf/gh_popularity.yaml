units:
  wraps:
  - name: SetoptconfGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: setoptconf
    run:
      justification:
      - type: WARNING
        message: Project 'setoptconf' has low popularity on GitHub
        link: https://github.com/jayclassless/setoptconf
        package_name: setoptconf
