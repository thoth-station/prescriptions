units:
  wraps:
    - name: TaGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: ta
      run:
        stack_info:
          - type: WARNING
            message: "Package 'ta' is hosted on GitHub"
            link: "https://github.com/bukosabino/ta"
