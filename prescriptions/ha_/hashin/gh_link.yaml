units:
  wraps:
    - name: HashinGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: hashin
      run:
        stack_info:
          - type: WARNING
            message: "Package 'hashin' is hosted on GitHub"
            link: "https://github.com/peterbe/hashin"
