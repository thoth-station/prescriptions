units:
  wraps:
    - name: PluggyGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pluggy
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pluggy' is hosted on GitHub"
            link: "https://github.com/pytest-dev/pluggy"
