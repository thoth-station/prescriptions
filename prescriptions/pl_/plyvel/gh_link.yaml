units:
  wraps:
    - name: PlyvelGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: plyvel
      run:
        stack_info:
          - type: WARNING
            message: "Package 'plyvel' is hosted on GitHub"
            link: "https://github.com/wbolster/plyvel"
