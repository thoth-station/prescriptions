units:
  wraps:
  - name: Colcon-test-resultGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: colcon
        package_version:
          index_url: https://pypi.org/simple
          name: colcon-test-result
        repository: colcon-test-result
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
