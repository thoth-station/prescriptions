units:
  wraps:
    - name: DiscordPySelfGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: discord-py-self
      run:
        stack_info:
          - type: WARNING
            message: "Package 'discord-py-self' is hosted on GitHub"
            link: "https://github.com/dolfies/discord.py-self"
