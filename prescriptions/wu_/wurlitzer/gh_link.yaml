units:
  wraps:
    - name: WurlitzerGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: wurlitzer
      run:
        stack_info:
          - type: WARNING
            message: "Package 'wurlitzer' is hosted on GitHub"
            link: "https://github.com/minrk/wurlitzer"
