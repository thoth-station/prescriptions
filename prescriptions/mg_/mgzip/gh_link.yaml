units:
  wraps:
    - name: MgzipGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: mgzip
      run:
        stack_info:
          - type: WARNING
            message: "Package 'mgzip' is hosted on GitHub"
            link: "https://github.com/vinlyx/mgzip"
