units:
  wraps:
    - name: WebsocketsGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: websockets
      run:
        stack_info:
          - type: WARNING
            message: "Package 'websockets' is hosted on GitHub"
            link: "https://github.com/aaugustin/websockets"
