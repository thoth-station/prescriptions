units:
  wraps:
    - name: DtaleGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: dtale
      run:
        stack_info:
          - type: WARNING
            message: "Package 'dtale' is hosted on GitHub"
            link: "https://github.com/man-group/dtale"
