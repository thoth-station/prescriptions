units:
  wraps:
    - name: CfscrapeGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: cfscrape
      run:
        stack_info:
          - type: WARNING
            message: "Package 'cfscrape' is hosted on GitHub"
            link: "https://github.com/Anorov/cloudflare-scrape"
