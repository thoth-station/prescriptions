units:
  wraps:
    - name: RobotremoteserverGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: robotremoteserver
      run:
        stack_info:
          - type: INFO
            message: "Package 'robotremoteserver' is hosted on GitHub"
            link: "https://github.com/robotframework/PythonRemoteServer"
