units:
  wraps:
    - name: RomanGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: roman
      run:
        stack_info:
          - type: WARNING
            message: "Package 'roman' is hosted on GitHub"
            link: "https://github.com/zopefoundation/roman"
