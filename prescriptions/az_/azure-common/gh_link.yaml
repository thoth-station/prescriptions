units:
  wraps:
    - name: AzureCommonGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: azure-common
      run:
        stack_info:
          - type: WARNING
            message: "Package 'azure-common' is hosted on GitHub"
            link: "https://github.com/Azure/azure-sdk-for-python"
