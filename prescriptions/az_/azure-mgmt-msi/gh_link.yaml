units:
  wraps:
    - name: AzureMgmtMsiGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: azure-mgmt-msi
      run:
        stack_info:
          - type: INFO
            message: "Package 'azure-mgmt-msi' is hosted on GitHub"
            link: "https://github.com/Azure/azure-sdk-for-python"
