units:
  wraps:
    - name: OrtoolsGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: ortools
      run:
        stack_info:
          - type: WARNING
            message: "Package 'ortools' is hosted on GitHub"
            link: "https://github.com/google/or-tools"
