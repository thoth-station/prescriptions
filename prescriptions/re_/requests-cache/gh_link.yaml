units:
  wraps:
    - name: RequestsCacheGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: requests-cache
      run:
        stack_info:
          - type: WARNING
            message: "Package 'requests-cache' is hosted on GitHub"
            link: "https://github.com/reclosedev/requests-cache"
