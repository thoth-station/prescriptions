units:
  wraps:
    - name: CoverallsGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: coveralls
      run:
        stack_info:
          - type: WARNING
            message: "Package 'coveralls' is hosted on GitHub"
            link: "https://github.com/TheKevJames/coveralls-python"
