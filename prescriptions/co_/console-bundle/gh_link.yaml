units:
  wraps:
    - name: ConsoleBundleGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: console-bundle
      run:
        stack_info:
          - type: WARNING
            message: "Package 'console-bundle' is hosted on GitHub"
            link: "https://github.com/pyfony/console-bundle"
