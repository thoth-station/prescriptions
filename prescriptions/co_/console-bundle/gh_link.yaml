units:
  wraps:
    - name: ConsoleBundleGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: console-bundle
      run:
        justification:
          - type: INFO
            message: "Package 'console-bundle' is hosted on GitHub"
            link: "https://github.com/pyfony/console-bundle"
            package_name: console-bundle
