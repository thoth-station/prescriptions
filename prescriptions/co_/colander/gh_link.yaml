units:
  wraps:
    - name: ColanderGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: colander
      run:
        stack_info:
          - type: WARNING
            message: "Package 'colander' is hosted on GitHub"
            link: "https://github.com/Pylons/colander"
