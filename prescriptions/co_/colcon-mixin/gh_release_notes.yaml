units:
  wraps:
  - name: ColconMixinGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: colcon
        package_version:
          index_url: https://pypi.org/simple
          name: colcon-mixin
        repository: colcon-mixin
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
