units:
  wraps:
    - name: ColconParallelExecutorGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: colcon-parallel-executor
      run:
        stack_info:
          - type: WARNING
            message: "Package 'colcon-parallel-executor' is hosted on GitHub"
            link: "https://github.com/colcon/colcon-parallel-executor"
