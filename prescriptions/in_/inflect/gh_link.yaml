units:
  wraps:
    - name: InflectGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: inflect
      run:
        stack_info:
          - type: INFO
            message: "Package 'inflect' is hosted on GitHub"
            link: "https://github.com/jaraco/inflect"
