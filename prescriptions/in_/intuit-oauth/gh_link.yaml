units:
  wraps:
    - name: IntuitOauthGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: intuit-oauth
      run:
        stack_info:
          - type: WARNING
            message: "Package 'intuit-oauth' is hosted on GitHub"
            link: "https://github.com/intuit/oauth-pythonclient"
