units:
  wraps:
    - name: IntervalsGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: intervals
      run:
        stack_info:
          - type: WARNING
            message: "Package 'intervals' is hosted on GitHub"
            link: "https://github.com/kvesteri/intervals"
