units:
  wraps:
    - name: IncrementalGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: incremental
      run:
        stack_info:
          - type: INFO
            message: "Package 'incremental' is hosted on GitHub"
            link: "https://github.com/twisted/incremental"
