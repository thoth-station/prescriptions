units:
  wraps:
    - name: InflectionGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: inflection
      run:
        stack_info:
          - type: INFO
            message: "Package 'inflection' is hosted on GitHub"
            link: "https://github.com/jpvanhal/inflection"
