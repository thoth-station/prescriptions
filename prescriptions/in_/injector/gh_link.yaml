units:
  wraps:
    - name: InjectorGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: injector
      run:
        stack_info:
          - type: WARNING
            message: "Package 'injector' is hosted on GitHub"
            link: "https://github.com/alecthomas/injector"
