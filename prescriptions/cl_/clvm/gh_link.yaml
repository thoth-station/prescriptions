units:
  wraps:
    - name: ClvmGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: clvm
      run:
        stack_info:
          - type: INFO
            message: "Package 'clvm' is hosted on GitHub"
            link: "https://github.com/Chia-Network/clvm"
