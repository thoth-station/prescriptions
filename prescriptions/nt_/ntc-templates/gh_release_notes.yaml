units:
  wraps:
  - name: Ntc-templatesGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: networktocode
        package_version:
          index_url: https://pypi.org/simple
          name: ntc-templates
        repository: ntc-templates
        tag_version_prefix: v
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
