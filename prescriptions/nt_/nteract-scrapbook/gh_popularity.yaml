units:
  wraps:
  - name: NteractScrapbookGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: nteract-scrapbook
    run:
      justification:
      - type: INFO
        message: Project 'nteract-scrapbook' has high popularity on GitHub
        link: https://github.com/nteract/scrapbook
        package_name: nteract-scrapbook
