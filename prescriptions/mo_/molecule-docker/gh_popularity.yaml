units:
  wraps:
  - name: MoleculeDockerGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: molecule-docker
    run:
      justification:
      - type: INFO
        message: Project 'molecule-docker' has moderate popularity on GitHub
        link: https://github.com/ansible-community/molecule-docker
        package_name: molecule-docker
