units:
  wraps:
    - name: MoleculeGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: molecule
      run:
        stack_info:
          - type: WARNING
            message: "Package 'molecule' is hosted on GitHub"
            link: "https://github.com/ansible-community/molecule"
