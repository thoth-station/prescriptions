units:
  wraps:
    - name: MoblyGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: mobly
      run:
        stack_info:
          - type: INFO
            message: "Package 'mobly' is hosted on GitHub"
            link: "https://github.com/google/mobly"
