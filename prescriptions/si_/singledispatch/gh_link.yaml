units:
  wraps:
    - name: SingledispatchGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: singledispatch
      run:
        stack_info:
          - type: INFO
            message: "Package 'singledispatch' is hosted on GitHub"
            link: "https://github.com/jaraco/singledispatch"
