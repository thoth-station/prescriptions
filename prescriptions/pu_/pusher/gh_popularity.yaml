units:
  wraps:
  - name: PusherGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: pusher
    run:
      justification:
      - type: INFO
        message: Project 'pusher' has high popularity on GitHub
        link: https://github.com/pusher/pusher-http-python
        package_name: pusher
