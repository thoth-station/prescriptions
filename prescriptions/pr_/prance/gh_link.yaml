units:
  wraps:
    - name: PranceGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: prance
      run:
        stack_info:
          - type: WARNING
            message: "Package 'prance' is hosted on GitHub"
            link: "https://github.com/RonnyPfannschmidt/prance"
