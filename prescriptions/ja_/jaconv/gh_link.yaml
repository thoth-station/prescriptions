units:
  wraps:
    - name: JaconvGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: jaconv
      run:
        stack_info:
          - type: WARNING
            message: "Package 'jaconv' is hosted on GitHub"
            link: "https://github.com/ikegami-yukino/jaconv"
