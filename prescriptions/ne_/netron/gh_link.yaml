units:
  wraps:
    - name: NetronGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: netron
      run:
        justification:
          - type: INFO
            message: "Package 'netron' is hosted on GitHub"
            link: "https://github.com/lutzroeder/netron"
            package_name: netron
