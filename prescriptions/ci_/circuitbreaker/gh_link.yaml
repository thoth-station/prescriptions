units:
  wraps:
    - name: CircuitbreakerGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: circuitbreaker
      run:
        stack_info:
          - type: INFO
            message: "Package 'circuitbreaker' is hosted on GitHub"
            link: "https://github.com/fabfuel/circuitbreaker"
