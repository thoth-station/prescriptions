units:
  wraps:
    - name: ImageioGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: imageio
      run:
        stack_info:
          - type: WARNING
            message: "Package 'imageio' is hosted on GitHub"
            link: "https://github.com/imageio/imageio"
