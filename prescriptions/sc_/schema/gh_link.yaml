units:
  wraps:
    - name: SchemaGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: schema
      run:
        stack_info:
          - type: WARNING
            message: "Package 'schema' is hosted on GitHub"
            link: "https://github.com/keleshev/schema"
