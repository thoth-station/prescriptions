units:
  wraps:
    - name: ScrapyGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: scrapy
      run:
        stack_info:
          - type: WARNING
            message: "Package 'scrapy' is hosted on GitHub"
            link: "https://github.com/scrapy/scrapy"
