units:
  wraps:
    - name: ScrampGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: scramp
      run:
        stack_info:
          - type: WARNING
            message: "Package 'scramp' is hosted on GitHub"
            link: "https://github.com/tlocke/scramp"
