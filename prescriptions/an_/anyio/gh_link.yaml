units:
  wraps:
    - name: AnyioGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: anyio
      run:
        stack_info:
          - type: WARNING
            message: "Package 'anyio' is hosted on GitHub"
            link: "https://github.com/agronholm/anyio"
