units:
  wraps:
    - name: FlatdictGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: flatdict
      run:
        stack_info:
          - type: WARNING
            message: "Package 'flatdict' is hosted on GitHub"
            link: "https://github.com/gmr/flatdict"
