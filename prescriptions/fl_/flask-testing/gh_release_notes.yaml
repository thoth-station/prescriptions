units:
  wraps:
  - name: FlaskTestingGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: jarus
        package_version:
          index_url: https://pypi.org/simple
          name: flask-testing
        repository: flask-testing
        tag_version_prefix: v
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
