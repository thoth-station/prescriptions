units:
  wraps:
    - name: FlaskBcryptGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: flask-bcrypt
      run:
        stack_info:
          - type: WARNING
            message: "Package 'flask-bcrypt' is hosted on GitHub"
            link: "https://github.com/maxcountryman/flask-bcrypt"
