units:
  wraps:
    - name: StackOverflowFlaskTagWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        state:
          resolved_dependencies:
            - name: flask
      run:
        justification:
          - type: INFO
            message: "See StackOverflow tags specific for Flask"
            link: https://stackoverflow.com/questions/tagged/flask
