units:
  wraps:
  - name: Flask-jwt-extendedGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: vimalloc
        package_version:
          index_url: https://pypi.org/simple
          name: flask-jwt-extended
        repository: flask-jwt-extended
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
