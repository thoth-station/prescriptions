units:
  wraps:
  - name: DockerptyGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: dockerpty
    run:
      justification:
      - type: INFO
        message: Project 'dockerpty' has high popularity on GitHub
        link: https://github.com/d11wtq/dockerpty
        package_name: dockerpty
