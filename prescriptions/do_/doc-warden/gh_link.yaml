units:
  wraps:
    - name: DocWardenGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: doc-warden
      run:
        stack_info:
          - type: WARNING
            message: "Package 'doc-warden' is hosted on GitHub"
            link: "https://github.com/Azure/azure-sdk-tools"
