units:
  wraps:
    - name: KeyboardGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: keyboard
      run:
        stack_info:
          - type: WARNING
            message: "Package 'keyboard' is hosted on GitHub"
            link: "https://github.com/boppreh/keyboard"
