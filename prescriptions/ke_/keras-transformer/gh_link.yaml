units:
  wraps:
    - name: KerasTransformerGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: keras-transformer
      run:
        justification:
          - type: INFO
            message: "Package 'keras-transformer' is hosted on GitHub"
            link: "https://github.com/CyberZHG/keras-transformer"
            package_name: keras-transformer
