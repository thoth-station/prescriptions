units:
  wraps:
    - name: KerasResnetGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: keras-resnet
      run:
        stack_info:
          - type: WARNING
            message: "Package 'keras-resnet' is hosted on GitHub"
            link: "https://github.com/broadinstitute/keras-resnet"
