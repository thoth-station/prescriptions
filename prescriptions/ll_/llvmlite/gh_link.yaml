units:
  wraps:
    - name: LlvmliteGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: llvmlite
      run:
        stack_info:
          - type: WARNING
            message: "Package 'llvmlite' is hosted on GitHub"
            link: "https://github.com/numba/llvmlite"
