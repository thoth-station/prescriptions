units:
  wraps:
    - name: SaspyGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: saspy
      run:
        stack_info:
          - type: INFO
            message: "Package 'saspy' is hosted on GitHub"
            link: "https://github.com/sassoftware/saspy"
