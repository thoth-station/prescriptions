units:
  sieves:
  - match:
      package_version:
        index_url: https://pypi.org/simple
        name: torchvision
        version: '!=0.10.0'
    name: RHODSTorchvisionSieve
    run:
      log:
        message: Removing package 'torchvision' as version does not match product
          version expected '==0.10.0'
        type: WARNING
      stack_info:
      - link: https://www.redhat.com/en/technologies/cloud-computing/openshift/openshift-data-science
        message: Removing package 'torchvision' as version does not match product
          version expected '==0.10.0'
        type: WARNING
    should_include:
      adviser_pipeline: true
      labels:
        consumer: rhods
    type: sieve
