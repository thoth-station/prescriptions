units:
  wraps:
    - name: TraitsGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: traits
      run:
        stack_info:
          - type: WARNING
            message: "Package 'traits' is hosted on GitHub"
            link: "https://github.com/enthought/traits"
