units:
  wraps:
    - name: DaphneGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: daphne
      run:
        stack_info:
          - type: WARNING
            message: "Package 'daphne' is hosted on GitHub"
            link: "https://github.com/django/daphne"
