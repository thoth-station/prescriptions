units:
  wraps:
    - name: DaskGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: dask
      run:
        justification:
          - type: INFO
            message: "Package 'dask' is hosted on GitHub"
            link: "https://github.com/dask/dask"
            package_name: dask
