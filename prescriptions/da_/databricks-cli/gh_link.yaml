units:
  wraps:
    - name: DatabricksCliGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: databricks-cli
      run:
        stack_info:
          - type: WARNING
            message: "Package 'databricks-cli' is hosted on GitHub"
            link: "https://github.com/databricks/databricks-cli"
