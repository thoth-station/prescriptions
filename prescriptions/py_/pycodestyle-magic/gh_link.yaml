units:
  wraps:
    - name: PycodestyleMagicGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pycodestyle-magic
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pycodestyle-magic' is hosted on GitHub"
            link: "https://github.com/mattijn/pycodestyle_magic"
