units:
  wraps:
    - name: PygerdutyGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pygerduty
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pygerduty' is hosted on GitHub"
            link: "https://github.com/dropbox/pygerduty"
