units:
  wraps:
  - name: PyreadstatGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: pyreadstat
    run:
      justification:
      - type: INFO
        message: Project 'pyreadstat' has high popularity on GitHub
        link: https://github.com/Roche/pyreadstat
        package_name: pyreadstat
