units:
  wraps:
    - name: PyyamlGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyyaml
      run:
        stack_info:
          - type: INFO
            message: "Package 'pyyaml' is hosted on GitHub"
            link: "https://github.com/yaml/pyyaml"
