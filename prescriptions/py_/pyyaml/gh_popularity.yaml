units:
  wraps:
  - name: PyyamlGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: pyyaml
    run:
      justification:
      - type: INFO
        message: Project 'pyyaml' has very high popularity on GitHub
        link: https://github.com/yaml/pyyaml
        package_name: pyyaml
