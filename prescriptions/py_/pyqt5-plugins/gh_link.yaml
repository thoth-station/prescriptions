units:
  wraps:
    - name: Pyqt5PluginsGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyqt5-plugins
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pyqt5-plugins' is hosted on GitHub"
            link: "https://github.com/altendky/pyqt-plugins"
