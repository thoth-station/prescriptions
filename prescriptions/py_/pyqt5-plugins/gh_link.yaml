units:
  wraps:
    - name: Pyqt5PluginsGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyqt5-plugins
      run:
        stack_info:
          - type: INFO
            message: "Package 'pyqt5-plugins' is hosted on GitHub"
            link: "https://github.com/altendky/pyqt-plugins"
