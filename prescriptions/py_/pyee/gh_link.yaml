units:
  wraps:
    - name: PyeeGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyee
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pyee' is hosted on GitHub"
            link: "https://github.com/jfhbrook/pyee"
