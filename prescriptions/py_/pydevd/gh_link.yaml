units:
  wraps:
    - name: PydevdGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pydevd
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pydevd' is hosted on GitHub"
            link: "https://github.com/fabioz/PyDev.Debugger"
