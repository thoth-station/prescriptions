units:
  wraps:
    - name: PysharkGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyshark
      run:
        stack_info:
          - type: INFO
            message: "Package 'pyshark' is hosted on GitHub"
            link: "https://github.com/KimiNewt/pyshark"
