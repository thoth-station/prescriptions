units:
  wraps:
    - name: PygtrieGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pygtrie
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pygtrie' is hosted on GitHub"
            link: "https://github.com/mina86/pygtrie"
