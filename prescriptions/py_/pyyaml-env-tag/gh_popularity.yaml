units:
  wraps:
  - name: PyyamlEnvTagGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: pyyaml-env-tag
    run:
      justification:
      - type: WARNING
        message: Project 'pyyaml-env-tag' has low popularity on GitHub
        link: https://github.com/waylan/pyyaml-env-tag
        package_name: pyyaml-env-tag
