units:
  wraps:
    - name: PytimeparseGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pytimeparse
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pytimeparse' is hosted on GitHub"
            link: "https://github.com/wroberts/pytimeparse"
