units:
  wraps:
    - name: PymssqlGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pymssql
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pymssql' is hosted on GitHub"
            link: "https://github.com/pymssql/pymssql"
