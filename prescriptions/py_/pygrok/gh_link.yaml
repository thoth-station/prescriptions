units:
  wraps:
    - name: PygrokGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pygrok
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pygrok' is hosted on GitHub"
            link: "https://github.com/garyelephant/pygrok"
