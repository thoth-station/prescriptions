units:
  wraps:
    - name: Pyroute2NftablesGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyroute2-nftables
      run:
        stack_info:
          - type: INFO
            message: "Package 'pyroute2-nftables' is hosted on GitHub"
            link: "https://github.com/svinota/pyroute2"
