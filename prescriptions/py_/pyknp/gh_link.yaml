units:
  wraps:
    - name: PyknpGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyknp
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pyknp' is hosted on GitHub"
            link: "https://github.com/ku-nlp/pyknp"
