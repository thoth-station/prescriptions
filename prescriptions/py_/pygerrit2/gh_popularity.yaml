units:
  wraps:
  - name: Pygerrit2GitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: pygerrit2
    run:
      justification:
      - type: INFO
        message: Project 'pygerrit2' has high popularity on GitHub
        link: https://github.com/dpursehouse/pygerrit2
        package_name: pygerrit2
