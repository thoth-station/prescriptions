units:
  wraps:
    - name: PyteGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyte
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pyte' is hosted on GitHub"
            link: "https://github.com/selectel/pyte"
