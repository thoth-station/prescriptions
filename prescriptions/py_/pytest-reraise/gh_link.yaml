units:
  wraps:
    - name: PytestReraiseGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pytest-reraise
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pytest-reraise' is hosted on GitHub"
            link: "https://github.com/bjoluc/pytest-reraise"
