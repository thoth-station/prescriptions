units:
  wraps:
    - name: PyorcGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyorc
      run:
        stack_info:
          - type: INFO
            message: "Package 'pyorc' is hosted on GitHub"
            link: "https://github.com/noirello/pyorc"
