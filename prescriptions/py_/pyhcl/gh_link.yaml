units:
  wraps:
    - name: PyhclGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyhcl
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pyhcl' is hosted on GitHub"
            link: "https://github.com/virtuald/pyhcl"
