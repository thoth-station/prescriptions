units:
  wraps:
    - name: PyrogramGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pyrogram
      run:
        stack_info:
          - type: INFO
            message: "Package 'pyrogram' is hosted on GitHub"
            link: "https://github.com/pyrogram/pyrogram"
