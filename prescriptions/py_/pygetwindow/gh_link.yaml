units:
  wraps:
    - name: PygetwindowGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pygetwindow
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pygetwindow' is hosted on GitHub"
            link: "https://github.com/asweigart/pygetwindow"
