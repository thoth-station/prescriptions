units:
  wraps:
    - name: PylatexencGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pylatexenc
      run:
        stack_info:
          - type: INFO
            message: "Package 'pylatexenc' is hosted on GitHub"
            link: "https://github.com/phfaist/pylatexenc"
