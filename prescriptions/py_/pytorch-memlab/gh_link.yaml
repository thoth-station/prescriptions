units:
  wraps:
    - name: PytorchMemlabGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pytorch-memlab
      run:
        stack_info:
          - type: INFO
            message: "Package 'pytorch-memlab' is hosted on GitHub"
            link: "https://github.com/Stonesjtu/pytorch_memlab"
