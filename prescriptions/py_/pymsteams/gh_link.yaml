units:
  wraps:
    - name: PymsteamsGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pymsteams
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pymsteams' is hosted on GitHub"
            link: "https://github.com/rveachkc/pymsteams"
