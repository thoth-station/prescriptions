units:
  wraps:
    - name: PyTrelloGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: py-trello
      run:
        stack_info:
          - type: WARNING
            message: "Package 'py-trello' is hosted on GitHub"
            link: "https://github.com/sarumont/py-trello"
