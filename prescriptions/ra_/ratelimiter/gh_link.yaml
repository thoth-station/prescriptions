units:
  wraps:
    - name: RatelimiterGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: ratelimiter
      run:
        stack_info:
          - type: WARNING
            message: "Package 'ratelimiter' is hosted on GitHub"
            link: "https://github.com/RazerM/ratelimiter"
