units:
  wraps:
    - name: EradicateGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: eradicate
      run:
        stack_info:
          - type: WARNING
            message: "Package 'eradicate' is hosted on GitHub"
            link: "https://github.com/myint/eradicate"
