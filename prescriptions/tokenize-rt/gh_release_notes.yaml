units:
  wraps:
  - name: Tokenize-rtGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: asottile
        package_version:
          index_url: https://pypi.org/simple
          name: tokenize-rt
        repository: tokenize-rt
        tag_version_prefix: v
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
