units:
  wraps:
  - name: PvcGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: pvc
    run:
      justification:
      - type: INFO
        message: Project 'pvc' has high popularity on GitHub
        link: https://github.com/dnaeon/pvc
        package_name: pvc
