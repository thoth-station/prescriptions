units:
  wraps:
    - name: VirtualenvWrapperDocsWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        state:
          resolved_dependencies:
            - name: virtualenvwrapper
      run:
        justification:
          - type: INFO
            message: "See virtualenvwrapper project documentation"
            link: "https://virtualenvwrapper.readthedocs.io/"
            package_name: virtualenvwrapper
