units:
  wraps:
    - name: VincentGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: vincent
      run:
        stack_info:
          - type: WARNING
            message: "Package 'vincent' is hosted on GitHub"
            link: "https://github.com/wrobstory/vincent"
