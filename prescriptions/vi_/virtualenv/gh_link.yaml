units:
  wraps:
    - name: VirtualenvGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: virtualenv
      run:
        stack_info:
          - type: WARNING
            message: "Package 'virtualenv' is hosted on GitHub"
            link: "https://github.com/pypa/virtualenv"
