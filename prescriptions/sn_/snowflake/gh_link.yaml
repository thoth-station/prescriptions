units:
  wraps:
    - name: SnowflakeGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: snowflake
      run:
        stack_info:
          - type: INFO
            message: "Package 'snowflake' is hosted on GitHub"
            link: "https://github.com/shaddi/snowflake"
