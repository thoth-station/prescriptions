units:
  wraps:
    - name: GttsTokenGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: gtts-token
      run:
        stack_info:
          - type: INFO
            message: "Package 'gtts-token' is hosted on GitHub"
            link: "https://github.com/boudewijn26/gTTS-token"
