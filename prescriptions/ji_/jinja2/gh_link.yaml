units:
  wraps:
    - name: Jinja2GitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: jinja2
      run:
        justification:
          - type: INFO
            message: "Package 'jinja2' is hosted on GitHub"
            link: "https://github.com/pallets/jinja"
            package_name: jinja2
