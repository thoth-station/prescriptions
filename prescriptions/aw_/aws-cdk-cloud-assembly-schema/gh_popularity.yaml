units:
  wraps:
  - name: AwsCdkCloudAssemblySchemaGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: aws-cdk-cloud-assembly-schema
    run:
      justification:
      - type: INFO
        message: Project 'aws-cdk-cloud-assembly-schema' has very high popularity on GitHub
        link: https://github.com/aws/aws-cdk
        package_name: aws-cdk-cloud-assembly-schema
