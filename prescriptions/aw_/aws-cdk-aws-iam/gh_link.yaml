units:
  wraps:
    - name: AwsCdkAwsIamGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: aws-cdk-aws-iam
      run:
        stack_info:
          - type: WARNING
            message: "Package 'aws-cdk-aws-iam' is hosted on GitHub"
            link: "https://github.com/aws/aws-cdk"
