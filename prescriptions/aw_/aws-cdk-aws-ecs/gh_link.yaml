units:
  wraps:
    - name: AwsCdkAwsEcsGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: aws-cdk-aws-ecs
      run:
        stack_info:
          - type: WARNING
            message: "Package 'aws-cdk-aws-ecs' is hosted on GitHub"
            link: "https://github.com/aws/aws-cdk"
