units:
  wraps:
    - name: GymGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: gym
      run:
        stack_info:
          - type: WARNING
            message: "Package 'gym' is hosted on GitHub"
            link: "https://github.com/openai/gym"
