units:
  wraps:
    - name: CheckdigitGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: checkdigit
      run:
        stack_info:
          - type: WARNING
            message: "Package 'checkdigit' is hosted on GitHub"
            link: "https://github.com/harens/checkdigit"
