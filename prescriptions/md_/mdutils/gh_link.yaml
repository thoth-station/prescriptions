units:
  wraps:
    - name: MdutilsGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: mdutils
      run:
        stack_info:
          - type: WARNING
            message: "Package 'mdutils' is hosted on GitHub"
            link: "https://github.com/didix21/"
