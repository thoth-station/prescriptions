units:
  wraps:
    - name: LaspyGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: laspy
      run:
        stack_info:
          - type: WARNING
            message: "Package 'laspy' is hosted on GitHub"
            link: "https://github.com/laspy/laspy"
