units:
  wraps:
    - name: LangdetectGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: langdetect
      run:
        stack_info:
          - type: WARNING
            message: "Package 'langdetect' is hosted on GitHub"
            link: "https://github.com/Mimino666/langdetect"
