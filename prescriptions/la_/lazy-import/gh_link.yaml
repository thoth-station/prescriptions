units:
  wraps:
    - name: LazyImportGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: lazy-import
      run:
        stack_info:
          - type: WARNING
            message: "Package 'lazy-import' is hosted on GitHub"
            link: "https://github.com/mnmelo/lazy_import"
