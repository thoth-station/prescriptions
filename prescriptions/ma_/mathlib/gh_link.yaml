units:
  wraps:
    - name: MathlibGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: mathlib
      run:
        stack_info:
          - type: WARNING
            message: "Package 'mathlib' is hosted on GitHub"
            link: "https://github.com/spapanik/mathlib"
