units:
  wraps:
    - name: MarkdownGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: markdown
      run:
        stack_info:
          - type: WARNING
            message: "Package 'markdown' is hosted on GitHub"
            link: "https://github.com/Python-Markdown/markdown"
