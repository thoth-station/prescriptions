units:
  wraps:
    - name: MagicattrGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: magicattr
      run:
        justification:
          - type: INFO
            message: "Package 'magicattr' is hosted on GitHub"
            link: "https://github.com/frmdstryr/magicattr"
            package_name: magicattr
