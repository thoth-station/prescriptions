units:
  wraps:
  - name: MapboxGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: mapbox
    run:
      justification:
      - type: INFO
        message: Project 'mapbox' has high popularity on GitHub
        link: https://github.com/mapbox/mapbox-sdk-py
        package_name: mapbox
