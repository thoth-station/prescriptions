units:
  wraps:
  - name: ZopeSchemaGitHubURLWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
    - state:
        resolved_dependencies:
        - name: zope-schema
    run:
      justification:
      - type: INFO
        message: "Package 'zope-schema' is hosted on GitHub"
        link: "https://github.com/zopefoundation/zope.schema"
        package_name: zope-schema
