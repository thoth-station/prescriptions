units:
  wraps:
  - name: ZopeSchemaGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: zope-schema
    run:
      justification:
      - type: INFO
        message: Project 'zope-schema' has moderate popularity on GitHub
        link: https://github.com/zopefoundation/zope.schema
        package_name: zope-schema
