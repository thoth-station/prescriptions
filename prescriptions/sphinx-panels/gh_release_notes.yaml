units:
  wraps:
  - name: Sphinx-panelsGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: executablebooks
        package_version:
          index_url: https://pypi.org/simple
          name: sphinx-panels
        repository: sphinx-panels
        tag_version_prefix: v
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
