units:
  wraps:
  - name: Tox-gh-actionsGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: ymyzk
        package_version:
          index_url: https://pypi.org/simple
          name: tox-gh-actions
        repository: tox-gh-actions
        tag_version_prefix: v
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
