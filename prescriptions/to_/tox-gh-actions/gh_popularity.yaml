units:
  wraps:
  - name: ToxGhActionsGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: tox-gh-actions
    run:
      justification:
      - type: INFO
        message: Project 'tox-gh-actions' has high popularity on GitHub
        link: https://github.com/ymyzk/tox-gh-actions
        package_name: tox-gh-actions
