units:
  wraps:
    - name: ToxDocsWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        state:
          resolved_dependencies:
            - name: tox
      run:
        justification:
          - type: INFO
            message: "See Tox project documentation"
            link: "https://tox.readthedocs.io/"
            package_name: tox
