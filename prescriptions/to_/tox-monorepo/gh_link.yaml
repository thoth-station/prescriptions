units:
  wraps:
    - name: ToxMonorepoGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: tox-monorepo
      run:
        stack_info:
          - type: WARNING
            message: "Package 'tox-monorepo' is hosted on GitHub"
            link: "https://github.com/Azure/azure-sdk-tools"
