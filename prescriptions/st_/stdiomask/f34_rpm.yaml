units:
  wraps:
    - name: StdiomaskFedora34RPMWrap
      type: wrap
      should_include:
        adviser_pipeline: true
        runtime_environments:
          operating_systems:
            - name: fedora
              version: "34"
      match:
        state:
          resolved_dependencies:
            - name: stdiomask
              index_url: 'https://pypi.org/simple'
      run:
        justification:
          - type: INFO
            message: "Package 'stdiomask' is packaged as an RPM in Fedora 34 as 'python-stdiomask'"
            link: https://src.fedoraproject.org/rpms/python-stdiomask
            package_name: stdiomask
