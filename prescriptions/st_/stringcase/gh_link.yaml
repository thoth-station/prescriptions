units:
  wraps:
    - name: StringcaseGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: stringcase
      run:
        stack_info:
          - type: INFO
            message: "Package 'stringcase' is hosted on GitHub"
            link: "https://github.com/okunishinishi/python-stringcase"
