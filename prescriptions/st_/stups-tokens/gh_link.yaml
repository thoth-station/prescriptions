units:
  wraps:
    - name: StupsTokensGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: stups-tokens
      run:
        stack_info:
          - type: WARNING
            message: "Package 'stups-tokens' is hosted on GitHub"
            link: "https://github.com/zalando-stups/python-tokens"
