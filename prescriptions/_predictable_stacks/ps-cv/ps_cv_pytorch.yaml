units:
  boots:
  - name: OpencvPythonPredictableStackPsCvPytorchBoot
    type: boot
    should_include:
      adviser_pipeline: true
      runtime_environments:
        base_images:
          not:
          - quay.io/thoth-station/ps-cv-pytorch
    match:
      package_name: opencv-python
    run:
      stack_info:
      - type: INFO
        message: Consider using a computer vision predictable stack 'ps-cv-pytorch' that has prepared environment with 'opencv-python'
        link: https://quay.io/repository/thoth-station/ps-cv-pytorch
  - name: PillowPredictableStackPsCvPytorchBoot
    type: boot
    should_include:
      adviser_pipeline: true
      runtime_environments:
        base_images:
          not:
          - quay.io/thoth-station/ps-cv-pytorch
    match:
      package_name: pillow
    run:
      stack_info:
      - type: INFO
        message: Consider using a computer vision predictable stack 'ps-cv-pytorch' that has prepared environment with 'pillow'
        link: https://quay.io/repository/thoth-station/ps-cv-pytorch
  - name: TensorboardPredictableStackPsCvPytorchBoot
    type: boot
    should_include:
      adviser_pipeline: true
      runtime_environments:
        base_images:
          not:
          - quay.io/thoth-station/ps-cv-pytorch
    match:
      package_name: tensorboard
    run:
      stack_info:
      - type: INFO
        message: Consider using a computer vision predictable stack 'ps-cv-pytorch' that has prepared environment with 'tensorboard'
        link: https://quay.io/repository/thoth-station/ps-cv-pytorch
  - name: TorchPredictableStackPsCvPytorchBoot
    type: boot
    should_include:
      adviser_pipeline: true
      runtime_environments:
        base_images:
          not:
          - quay.io/thoth-station/ps-cv-pytorch
    match:
      package_name: torch
    run:
      stack_info:
      - type: INFO
        message: Consider using a computer vision predictable stack 'ps-cv-pytorch' that has prepared environment with 'torch'
        link: https://quay.io/repository/thoth-station/ps-cv-pytorch
  - name: TorchvisionPredictableStackPsCvPytorchBoot
    type: boot
    should_include:
      adviser_pipeline: true
      runtime_environments:
        base_images:
          not:
          - quay.io/thoth-station/ps-cv-pytorch
    match:
      package_name: torchvision
    run:
      stack_info:
      - type: INFO
        message: Consider using a computer vision predictable stack 'ps-cv-pytorch' that has prepared environment with 'torchvision'
        link: https://quay.io/repository/thoth-station/ps-cv-pytorch
  - name: JupyterTensorboardPredictableStackPsCvPytorchBoot
    type: boot
    should_include:
      adviser_pipeline: true
      runtime_environments:
        base_images:
          not:
          - quay.io/thoth-station/ps-cv-pytorch
    match:
      package_name: jupyter-tensorboard
    run:
      stack_info:
      - type: INFO
        message: Consider using a computer vision predictable stack 'ps-cv-pytorch' that has prepared environment with 'jupyter-tensorboard'
        link: https://quay.io/repository/thoth-station/ps-cv-pytorch
