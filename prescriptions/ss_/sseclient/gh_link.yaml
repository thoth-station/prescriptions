units:
  wraps:
    - name: SseclientGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: sseclient
      run:
        stack_info:
          - type: INFO
            message: "Package 'sseclient' is hosted on GitHub"
            link: "https://github.com/btubbs/sseclient"
