units:
  wraps:
    - name: DjangoCompatGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: django-compat
      run:
        stack_info:
          - type: WARNING
            message: "Package 'django-compat' is hosted on GitHub"
            link: "https://github.com/arteria/django-compat"
