units:
  wraps:
    - name: DjangoOauthToolkitGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: django-oauth-toolkit
      run:
        stack_info:
          - type: WARNING
            message: "Package 'django-oauth-toolkit' is hosted on GitHub"
            link: "https://github.com/jazzband/django-oauth-toolkit"
