units:
  wraps:
  - name: DjangoAppconfGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: django-appconf
    run:
      justification:
      - type: INFO
        message: Project 'django-appconf' has high popularity on GitHub
        link: https://github.com/django-compressor/django-appconf
        package_name: django-appconf
