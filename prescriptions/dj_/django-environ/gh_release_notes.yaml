units:
  wraps:
  - name: DjangoEnvironGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: joke2k
        package_version:
          index_url: https://pypi.org/simple
          name: django-environ
        repository: django-environ
        tag_version_prefix: v
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
