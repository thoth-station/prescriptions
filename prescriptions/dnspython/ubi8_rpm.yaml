units:
  wraps:
    - name: DnspythonUbi8RPMWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        state:
          resolved_dependencies:
            - name: dnspython
              index_url: 'https://pypi.org/simple'
      run:
        stack_info:
          - type: INFO
            message: "Package 'dnspython' is packaged as an RPM in UBI8 as 'python3-dns'"
            link: rpm
