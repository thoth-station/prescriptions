units:
  wraps:
    - name: GoogleappenginecloudstorageclientGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: googleappenginecloudstorageclient
      run:
        stack_info:
          - type: INFO
            message: "Package 'googleappenginecloudstorageclient' is hosted on GitHub"
            link: "https://github.com/GoogleCloudPlatform/appengine-gcs-client"
