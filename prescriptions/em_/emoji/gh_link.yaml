units:
  wraps:
    - name: EmojiGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: emoji
      run:
        stack_info:
          - type: WARNING
            message: "Package 'emoji' is hosted on GitHub"
            link: "https://github.com/carpedm20/emoji"
