units:
  wraps:
    - name: LockfileFedora34RPMWrap
      type: wrap
      should_include:
        adviser_pipeline: true
        runtime_environments:
          operating_systems:
            - name: fedora
              version: "34"
      match:
        state:
          resolved_dependencies:
            - name: lockfile
              index_url: 'https://pypi.org/simple'
      run:
        justification:
          - type: INFO
            message: "Package 'lockfile' is packaged as an RPM in Fedora 34 as 'python-lockfile'"
            link: https://src.fedoraproject.org/rpms/python-lockfile
            package_name: lockfile
