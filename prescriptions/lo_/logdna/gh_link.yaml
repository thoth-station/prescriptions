units:
  wraps:
    - name: LogdnaGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: logdna
      run:
        stack_info:
          - type: WARNING
            message: "Package 'logdna' is hosted on GitHub"
            link: "https://github.com/logdna/python"
