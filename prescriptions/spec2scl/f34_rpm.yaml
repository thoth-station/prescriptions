units:
  wraps:
    - name: Spec2sclFedora34RPMWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        state:
          resolved_dependencies:
            - name: spec2scl
              index_url: 'https://pypi.org/simple'
      run:
        stack_info:
          - type: INFO
            message: "Package 'spec2scl' is packaged as an RPM in Fedora 34 as 'spec2scl'"
            link: https://src.fedoraproject.org/rpms/spec2scl
