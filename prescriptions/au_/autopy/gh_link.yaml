units:
  wraps:
    - name: AutopyGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: autopy
      run:
        stack_info:
          - type: WARNING
            message: "Package 'autopy' is hosted on GitHub"
            link: "https://github.com/autopilot-rs/autopy"
