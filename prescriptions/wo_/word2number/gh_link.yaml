units:
  wraps:
    - name: Word2numberGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: word2number
      run:
        stack_info:
          - type: WARNING
            message: "Package 'word2number' is hosted on GitHub"
            link: "https://github.com/akshaynagpal/w2n"
