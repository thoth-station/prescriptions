units:
  wraps:
  - name: AsyncExitStackGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: sorcio
        package_version:
          index_url: https://pypi.org/simple
          name: async-exit-stack
        repository: async_exit_stack
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
