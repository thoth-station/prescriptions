units:
  wraps:
    - name: HttpcoreGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: httpcore
      run:
        stack_info:
          - type: WARNING
            message: "Package 'httpcore' is hosted on GitHub"
            link: "https://github.com/encode/httpcore"
