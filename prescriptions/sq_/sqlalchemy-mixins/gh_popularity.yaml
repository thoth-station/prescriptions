units:
  wraps:
  - name: SqlalchemyMixinsGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: sqlalchemy-mixins
    run:
      justification:
      - type: INFO
        message: Project 'sqlalchemy-mixins' has very high popularity on GitHub
        link: https://github.com/absent1706/sqlalchemy-mixins
        package_name: sqlalchemy-mixins
