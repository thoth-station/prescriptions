units:
  wraps:
    - name: SqlMetadataGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: sql-metadata
      run:
        stack_info:
          - type: WARNING
            message: "Package 'sql-metadata' is hosted on GitHub"
            link: "https://github.com/macbre/sql-metadata"
