units:
  wraps:
  - name: SqlalchemyFiltersGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: sqlalchemy-filters
    run:
      justification:
      - type: INFO
        message: Project 'sqlalchemy-filters' has high popularity on GitHub
        link: https://github.com/juliotrigo/sqlalchemy-filters
        package_name: sqlalchemy-filters
