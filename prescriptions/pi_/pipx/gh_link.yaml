units:
  wraps:
    - name: PipxGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pipx
      run:
        stack_info:
          - type: INFO
            message: "Package 'pipx' is hosted on GitHub"
            link: "https://github.com/pypa/pipx"
