units:
  wraps:
  - name: PipenvSetupGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: pipenv-setup
    run:
      justification:
      - type: INFO
        message: Project 'pipenv-setup' has high popularity on GitHub
        link: https://github.com/Madoshakalaka/pipenv-setup
        package_name: pipenv-setup
