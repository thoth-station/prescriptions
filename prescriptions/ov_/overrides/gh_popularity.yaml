units:
  wraps:
  - name: OverridesGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: overrides
    run:
      justification:
      - type: INFO
        message: Project 'overrides' has high popularity on GitHub
        link: https://github.com/mkorpela/overrides
        package_name: overrides
