units:
  wraps:
  - name: EnumToolsGitHubURLWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: enum-tools
    run:
      justification:
      - type: INFO
        message: Package 'enum-tools' is hosted on GitHub
        link: https://github.com/domdfcoding/enum_tools
        package_name: enum-tools
