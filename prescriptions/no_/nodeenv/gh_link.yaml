units:
  wraps:
    - name: NodeenvGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: nodeenv
      run:
        stack_info:
          - type: WARNING
            message: "Package 'nodeenv' is hosted on GitHub"
            link: "https://github.com/ekalinin/nodeenv"
