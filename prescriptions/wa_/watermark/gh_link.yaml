units:
  wraps:
    - name: WatermarkGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: watermark
      run:
        stack_info:
          - type: WARNING
            message: "Package 'watermark' is hosted on GitHub"
            link: "https://github.com/rasbt/watermark"
