units:
  wraps:
    - name: PemGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pem
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pem' is hosted on GitHub"
            link: "https://github.com/hynek/pem"
