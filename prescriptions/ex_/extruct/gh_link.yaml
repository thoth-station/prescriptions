units:
  wraps:
    - name: ExtructGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: extruct
      run:
        stack_info:
          - type: WARNING
            message: "Package 'extruct' is hosted on GitHub"
            link: "https://github.com/scrapinghub/extruct"
