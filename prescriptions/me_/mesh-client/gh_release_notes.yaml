units:
  wraps:
  - name: MeshClientGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: jamespic
        package_version:
          index_url: https://pypi.org/simple
          name: mesh-client
        repository: mesh-client
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
