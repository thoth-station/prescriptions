units:
  wraps:
    - name: Fpdf2GitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: fpdf2
      run:
        stack_info:
          - type: INFO
            message: "Package 'fpdf2' is hosted on GitHub"
            link: "https://github.com/PyFPDF/fpdf2"
