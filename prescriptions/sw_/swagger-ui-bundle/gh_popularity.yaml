units:
  wraps:
  - name: SwaggerUiBundleGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: swagger-ui-bundle
    run:
      justification:
      - type: INFO
        message: Project 'swagger-ui-bundle' has moderate popularity on GitHub
        link: https://github.com/dtkav/swagger_ui_bundle
        package_name: swagger-ui-bundle
