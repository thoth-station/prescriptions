units:
  wraps:
  - name: SwaggerSpecValidatorGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: swagger-spec-validator
    run:
      justification:
      - type: INFO
        message: Project 'swagger-spec-validator' has high popularity on GitHub
        link: https://github.com/Yelp/swagger_spec_validator
        package_name: swagger-spec-validator
