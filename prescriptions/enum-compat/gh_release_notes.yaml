units:
  wraps:
  - name: Enum-compatGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: jstasiak
        package_version:
          index_url: https://pypi.org/simple
          name: enum-compat
        repository: enum-compat
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
