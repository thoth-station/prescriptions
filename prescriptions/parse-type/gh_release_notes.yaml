units:
  wraps:
  - name: Parse-typeGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: jenisys
        package_version:
          index_url: https://pypi.org/simple
          name: parse-type
        repository: parse_type
        tag_version_prefix: v
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
