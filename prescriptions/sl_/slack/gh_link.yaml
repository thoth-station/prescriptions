units:
  wraps:
    - name: SlackGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: slack
      run:
        stack_info:
          - type: WARNING
            message: "Package 'slack' is hosted on GitHub"
            link: "https://github.com/zweifisch/slack"
