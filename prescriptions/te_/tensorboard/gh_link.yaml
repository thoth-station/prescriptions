units:
  wraps:
    - name: TensorboardGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: tensorboard
      run:
        stack_info:
          - type: WARNING
            message: "Package 'tensorboard' is hosted on GitHub"
            link: "https://github.com/tensorflow/tensorboard"
