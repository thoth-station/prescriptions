units:
  wraps:
    - name: TextractGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: textract
      run:
        stack_info:
          - type: WARNING
            message: "Package 'textract' is hosted on GitHub"
            link: "https://github.com/deanmalmgren/textract"
