units:
  wraps:
    - name: XstaticGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: xstatic
      run:
        stack_info:
          - type: WARNING
            message: "Package 'xstatic' is hosted on GitHub"
            link: "https://github.com/xstatic-py/xstatic"
