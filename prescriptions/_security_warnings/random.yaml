units:
  wraps:
    - name: RandomSecurityWarnings
      type: wrap
      should_include:
        adviser_pipeline: true
        library_usage:
          random:
            - random.*
      run:
        stack_info:
          - type: WARNING
            message: "random shouldn’t be used for security purposes, use secrets instead"
            link: "https://docs.python.org/3.11/library/secrets.html#module-secrets"
