units:
  wraps:
    - name: AttrdictGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: attrdict
      run:
        stack_info:
          - type: INFO
            message: "Package 'attrdict' is hosted on GitHub"
            link: "https://github.com/bcj/AttrDict"
