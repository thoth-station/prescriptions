units:
  wraps:
    - name: CeleryRedbeatGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: celery-redbeat
      run:
        stack_info:
          - type: WARNING
            message: "Package 'celery-redbeat' is hosted on GitHub"
            link: "https://github.com/sibson/redbeat"
