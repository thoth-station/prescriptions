units:
  boots:
  - name: PrescriptionAuthenticatedBoot
    type: boot
    should_include:
      adviser_pipeline: true
      authenticated: true
    run:
      stack_info:
      - type: INFO
        message: Resolver is running in authenticated mode
        link: https://github.com/thoth-station/prescriptions
      log:
        message: Resolver is running in authenticated mode
        type: INFO
