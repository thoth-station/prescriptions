units:
  wraps:
    - name: StackOverflowCUDATagWrap
      type: wrap
      should_include:
        adviser_pipeline: true
        runtime_environments:
          cuda_version: ">=0.0"
      run:
        justification:
          - type: INFO
            message: "See StackOverflow tags specific for CUDA"
            link: https://stackoverflow.com/questions/tagged/python+cuda
