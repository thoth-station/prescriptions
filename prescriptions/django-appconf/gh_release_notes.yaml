units:
  wraps:
  - name: Django-appconfGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: django-compressor
        package_version:
          index_url: https://pypi.org/simple
          name: django-appconf
        repository: django-appconf
        tag_version_prefix: v
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
