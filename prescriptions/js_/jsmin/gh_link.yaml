units:
  wraps:
    - name: JsminGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: jsmin
      run:
        stack_info:
          - type: WARNING
            message: "Package 'jsmin' is hosted on GitHub"
            link: "https://github.com/tikitu/jsmin"
