units:
  wraps:
    - name: JsonsirGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: jsonsir
      run:
        stack_info:
          - type: WARNING
            message: "Package 'jsonsir' is hosted on GitHub"
            link: "https://github.com/RussellLuo/jsonsir"
