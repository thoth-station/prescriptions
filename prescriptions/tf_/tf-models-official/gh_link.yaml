units:
  wraps:
    - name: TfModelsOfficialGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: tf-models-official
      run:
        stack_info:
          - type: WARNING
            message: "Package 'tf-models-official' is hosted on GitHub"
            link: "https://github.com/tensorflow/models"
