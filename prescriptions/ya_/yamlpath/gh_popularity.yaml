units:
  wraps:
  - name: YamlpathGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: yamlpath
    run:
      justification:
      - type: INFO
        message: Project 'yamlpath' has moderate popularity on GitHub
        link: https://github.com/wwkimball/yamlpath
        package_name: yamlpath
