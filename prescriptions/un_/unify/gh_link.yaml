units:
  wraps:
    - name: UnifyGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: unify
      run:
        stack_info:
          - type: WARNING
            message: "Package 'unify' is hosted on GitHub"
            link: "https://github.com/myint/unify"
