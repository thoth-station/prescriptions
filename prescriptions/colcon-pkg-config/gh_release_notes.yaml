units:
  wraps:
  - name: Colcon-pkg-configGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: colcon
        package_version:
          index_url: https://pypi.org/simple
          name: colcon-pkg-config
        repository: colcon-pkg-config
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
