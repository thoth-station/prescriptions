units:
  wraps:
    - name: Boto3GitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: boto3
      run:
        stack_info:
          - type: WARNING
            message: "Package 'boto3' is hosted on GitHub"
            link: "https://github.com/boto/boto3"
