units:
  wraps:
  - name: SpeedtestCliGitHubReleaseNotesWrap
    run:
      release_notes:
      - organization: sivel
        package_version:
          index_url: https://pypi.org/simple
          name: speedtest-cli
        repository: speedtest-cli
        tag_version_prefix: v
    should_include:
      adviser_pipeline: true
    type: wrap.GitHubReleaseNotes
