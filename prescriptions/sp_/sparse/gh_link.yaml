units:
  wraps:
    - name: SparseGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: sparse
      run:
        stack_info:
          - type: WARNING
            message: "Package 'sparse' is hosted on GitHub"
            link: "https://github.com/pydata/sparse"
