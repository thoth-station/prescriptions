units:
  wraps:
    - name: BlackGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: black
      run:
        stack_info:
          - type: WARNING
            message: "Package 'black' is hosted on GitHub"
            link: "https://github.com/psf/black"
