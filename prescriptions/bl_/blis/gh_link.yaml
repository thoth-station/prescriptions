units:
  wraps:
    - name: BlisGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: blis
      run:
        stack_info:
          - type: WARNING
            message: "Package 'blis' is hosted on GitHub"
            link: "https://github.com/explosion/cython-blis"
