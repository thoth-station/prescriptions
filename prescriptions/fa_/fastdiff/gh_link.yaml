units:
  wraps:
    - name: FastdiffGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: fastdiff
      run:
        stack_info:
          - type: WARNING
            message: "Package 'fastdiff' is hosted on GitHub"
            link: "https://github.com/syrusakbary/fastdiff"
