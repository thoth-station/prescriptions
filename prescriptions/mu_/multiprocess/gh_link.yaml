units:
  wraps:
    - name: MultiprocessGitHubURLWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: multiprocess
      run:
        justification:
          - type: INFO
            message: "Package 'multiprocess' is hosted on GitHub"
            link: "https://github.com/uqfoundation/multiprocess"
            package_name: multiprocess
