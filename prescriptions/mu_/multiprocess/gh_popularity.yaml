units:
  wraps:
  - name: MultiprocessGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: multiprocess
    run:
      justification:
      - type: INFO
        message: Project 'multiprocess' has high popularity on GitHub
        link: https://github.com/uqfoundation/multiprocess
        package_name: multiprocess
