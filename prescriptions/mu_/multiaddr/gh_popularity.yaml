units:
  wraps:
  - name: MultiaddrGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: multiaddr
    run:
      justification:
      - type: INFO
        message: Project 'multiaddr' has moderate popularity on GitHub
        link: https://github.com/multiformats/py-multiaddr
        package_name: multiaddr
