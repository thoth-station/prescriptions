units:
  wraps:
  - name: MultivolumefileGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: multivolumefile
    run:
      justification:
      - type: WARNING
        message: Project 'multivolumefile' has low popularity on GitHub
        link: https://github.com/miurahr/multivolume
        package_name: multivolumefile
