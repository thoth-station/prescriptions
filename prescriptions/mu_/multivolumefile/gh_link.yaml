units:
  wraps:
    - name: MultivolumefileGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: multivolumefile
      run:
        stack_info:
          - type: WARNING
            message: "Package 'multivolumefile' is hosted on GitHub"
            link: "https://github.com/miurahr/multivolume"
