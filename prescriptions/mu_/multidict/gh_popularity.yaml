units:
  wraps:
  - name: MultidictGitHubPopularityWrap
    type: wrap
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: multidict
    run:
      justification:
      - type: INFO
        message: Project 'multidict' has high popularity on GitHub
        link: https://github.com/aio-libs/multidict
        package_name: multidict
