units:
  wraps:
    - name: PatchGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: patch
      run:
        stack_info:
          - type: WARNING
            message: "Package 'patch' is hosted on GitHub"
            link: "https://github.com/techtonik/python-patch"
