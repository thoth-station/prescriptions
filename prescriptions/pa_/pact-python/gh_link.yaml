units:
  wraps:
    - name: PactPythonGitHubURWrap
      type: wrap
      should_include:
        adviser_pipeline: true
      match:
        - state:
            resolved_dependencies:
              - name: pact-python
      run:
        stack_info:
          - type: WARNING
            message: "Package 'pact-python' is hosted on GitHub"
            link: "https://github.com/pact-foundation/pact-python"
